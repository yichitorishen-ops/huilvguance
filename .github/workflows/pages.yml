name: Collect Data and Deploy Pages

on:
  schedule:
    # Beijing 00:07-00:57 retries, Tuesday-Saturday; records the 00:00 slot.
    - cron: "7 16 * * 1-5"
    - cron: "17 16 * * 1-5"
    - cron: "27 16 * * 1-5"
    - cron: "37 16 * * 1-5"
    - cron: "47 16 * * 1-5"
    - cron: "57 16 * * 1-5"
    # Beijing 06:07-06:57 retries, Monday-Friday; records the 06:00 slot.
    - cron: "7 22 * * 0-4"
    - cron: "17 22 * * 0-4"
    - cron: "27 22 * * 0-4"
    - cron: "37 22 * * 0-4"
    - cron: "47 22 * * 0-4"
    - cron: "57 22 * * 0-4"
    # Beijing 13:07-13:57 retries, Monday-Friday; records the 13:00 slot.
    - cron: "7 5 * * 1-5"
    - cron: "17 5 * * 1-5"
    - cron: "27 5 * * 1-5"
    - cron: "37 5 * * 1-5"
    - cron: "47 5 * * 1-5"
    - cron: "57 5 * * 1-5"
    # Beijing 18:07-18:57 retries, Monday-Friday; records the 18:00 slot.
    - cron: "7 10 * * 1-5"
    - cron: "17 10 * * 1-5"
    - cron: "27 10 * * 1-5"
    - cron: "37 10 * * 1-5"
    - cron: "47 10 * * 1-5"
    - cron: "57 10 * * 1-5"
    # Frequent backstop; fills recently missed slots without overwriting complete slots.
    - cron: "8,18,28,38,48,58 * * * *"
  workflow_dispatch:
    inputs:
      collect:
        description: "Collect a data slot before deploying"
        required: true
        type: boolean
        default: false
      scheduled_hour:
        description: "Beijing scheduled hour to collect when collect is true"
        required: false
        type: choice
        default: "6"
        options:
          - "0"
          - "6"
          - "13"
          - "18"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages-data
  cancel-in-progress: false

jobs:
  collect-build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run tests
        run: python -m unittest discover -v

      - name: Resolve collection slot
        shell: bash
        run: |
          run_collection="false"
          run_catch_up="false"
          scheduled_hour=""
          schedule_cron=""
          backstop_cron="8,18,28,38,48,58 * * * *"

          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            if [[ "${{ github.event.schedule }}" == "$backstop_cron" ]]; then
              run_catch_up="true"
            else
              run_collection="true"
              schedule_cron="${{ github.event.schedule }}"
            fi
          elif [[ "${{ inputs.collect }}" == "true" ]]; then
            run_collection="true"
            scheduled_hour="${{ inputs.scheduled_hour }}"
          fi

          echo "RUN_COLLECTION=$run_collection" >> "$GITHUB_ENV"
          echo "RUN_CATCH_UP=$run_catch_up" >> "$GITHUB_ENV"
          echo "SCHEDULED_HOUR=$scheduled_hour" >> "$GITHUB_ENV"
          echo "SCHEDULE_CRON=$schedule_cron" >> "$GITHUB_ENV"

      - name: Collect data
        if: env.RUN_COLLECTION == 'true'
        shell: bash
        run: |
          if [[ -n "$SCHEDULE_CRON" ]]; then
            python scripts/collect_once.py --schedule-cron "$SCHEDULE_CRON"
          else
            python scripts/collect_once.py --scheduled-hour "$SCHEDULED_HOUR"
          fi

      - name: Catch up missing data
        if: env.RUN_CATCH_UP == 'true'
        run: python scripts/collect_once.py --catch-up --max-delay-minutes 120

      - name: Build static site
        run: python scripts/build_static_site.py --output dist

      - name: Commit data history
        if: env.RUN_COLLECTION == 'true' || env.RUN_CATCH_UP == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f data/sqlite.db
          if git diff --cached --quiet; then
            echo "No database changes to commit."
          else
            git commit -m "chore: update market data"
            git pull --rebase
            git push
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
